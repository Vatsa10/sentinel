# Sentinel grid credentials. The gateway began requiring these mid-challenge:
# RTSP returns 401 without them and the portal login needs email + password.
NETRA_GRID_EMAIL=your-registered@email.com
NETRA_GRID_PASSWORD=your-access-password