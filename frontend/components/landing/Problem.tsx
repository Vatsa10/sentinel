export function Problem() {
  return (
    <section className="mx-auto max-w-4xl px-6 py-16 text-center">
      <h2 className="text-2xl font-bold text-text sm:text-3xl">
        26 departments · ~80,000 cameras · no common view
      </h2>
      <p className="mx-auto mt-4 max-w-2xl text-muted">
        Every department runs its own CCTV, in its own viewer, on its own
        network. Finding one vehicle across feeds means phone calls, USB
        drives and hours of manual review — while the trail goes cold.
      </p>
    </section>
  );
}
